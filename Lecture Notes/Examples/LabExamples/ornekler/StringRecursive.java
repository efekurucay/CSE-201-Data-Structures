
import java.util.ArrayList;

public class StringRecursive {
    public static void main(String[] args) {
        
    }
    
    public static boolean func(String str, int vows, int cons) {
        str = str.toLowerCase();
        if(str.length() == 0) return cons > vows;
        ArrayList<String> consonants = new ArrayList<>("b", "c", "d", "f", "g", "h", "j", "k", "l", "m", "n", "p", "q", "r", "s", "t", "v", "w", "x", "y", "z");
        consonants.
        if(str.length() == 1) {
            if(consonants.indexOf(str.substring(0, 1)) != -1)
        }
        
    }
}
